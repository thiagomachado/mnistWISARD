class Ram {

    //to do: utilizar map
    addresses : Array<number>;


    constructor(inputSize : number) 
    {       
       this.addresses  = new Array(inputSize); 
    }
    
}

export {Ram as Ram};
